query fileByLocation($location: String!, $showHiddenFiles: Boolean!) {
    fileByLocation(location: $location, showHiddenFiles: $showHiddenFiles) {
        location,
        tags {
            name
            value
        }

        connections {
            name,
            value,
            to
        }
    }
}