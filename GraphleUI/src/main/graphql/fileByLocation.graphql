query fileByLocation($location: String!) {
    fileByLocation(location: $location) {
        location,
        tags {
            name
            value
        }

        connections {
            name,
            value,
            to
        }
    }
}